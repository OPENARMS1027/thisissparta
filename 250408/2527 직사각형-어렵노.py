'''
두 개의 직사각형의 좌표를 받은 후 겹치는 것이
직사각형, 선분, 점, 없음 인 경우 중 어떤 것인지 판별하기
'''

for _ in range(4):
    x_1,y_1,p_1,q_1,x_2,y_2,p_2,q_2 = map(int,input().split())

    # 직사각형일 때
    
    # 선분일 떄
    # 점일 때
    # 겹치는게 없을 때